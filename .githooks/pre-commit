#!/bin/sh
staged=$(git diff --cached --name-only --diff-filter=ACMR)

if printf '%s\n' "$staged" | grep -q '^docs/'; then
  echo "pre-commit: docs/ is local only" >&2
  exit 1
fi
if printf '%s\n' "$staged" | grep -qE '\.ts$'; then
  deno task check || exit 1
fi
